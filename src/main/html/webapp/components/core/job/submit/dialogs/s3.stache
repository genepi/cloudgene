<h4>Import data from S3 bucket</h4>
<p>Please enter the S3 bucket of your input folder. </p>
<form>
    <textarea class="form-control" id="buckets" placeholder="s3://my-bucket/input-folder" rows="8" name="buckets" width="30" height="20">{{value}}</textarea>
    <small class="text-muted">Enter a the location of a s3 folder or file. To specify more than one file, separate the files with a space or new line.</small>
</form>
